$(document).ready(function(){
  var width = 800;
  var height = 600;
  var padding = 40;
  var svg = d3.select("body")
  .append("svg")
  .attr("width",width)
  .attr("height",height);

  var data = [33,40,30];
  
});
