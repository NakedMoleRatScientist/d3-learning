$(document).ready(function() {
  var width = 800;
  var height = 600;
  console.log("Hello World");

  var svg = d3.select("body")
    .append("svg")
    .attr("width",width)
    .attr("height",height);
});
